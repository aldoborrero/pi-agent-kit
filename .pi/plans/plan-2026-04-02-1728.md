Plan:
1. `extensions/prompt-suggestion/index.ts` — create a new extension that tracks `currentSuggestion`, `enabled`, and in-flight generation state; on `agent_end`, collect the latest user/assistant context and generate a short “next prompt” suggestion with a configurable model; on `agent_start` / `session_start` / `session_switch`, clear stale suggestion state and refresh the widget.
2. `extensions/prompt-suggestion/index.ts` — render the suggestion in a widget (not status) and add commands such as `/suggestion` (toggle/status) and `/use-oracle`; implement acceptance with `ctx.ui.setEditorText(...)` so the suggestion is inserted into the editor rather than sent immediately.
3. `extensions/prompt-suggestion/index.ts` — reuse pi editor APIs confirmed in `.scratch/pi-mono/packages/coding-agent/src/core/extensions/types.ts:177-183` (`pasteToEditor`, `setEditorText`, `getEditorText`) and runtime wiring in `.scratch/pi-mono/packages/coding-agent/src/modes/interactive/interactive-mode.ts:1608-1610`; prefer `setEditorText` for the initial MVP, optionally preserving existing editor content if non-empty.
4. `extensions/prompt-suggestion/index.ts` — make the model configurable via env/config fallback (e.g. `PI_PROMPT_SUGGESTION_MODEL`, default `groq/openai/gpt-oss-20b`) and create the generation path with pi SDK primitives (`createAgentSession` / `ModelRegistry`) instead of shelling out.
5. `extensions/prompt-suggestion/index.ts` — port the core OpenClaude prompt/filters from `.scratch/openclaude/src/services/PromptSuggestion/promptSuggestion.ts:258-351` (generation prompt + forked suggestion logic) and `.scratch/openclaude/src/services/PromptSuggestion/promptSuggestion.ts:354-450` (filter rules), but skip speculation for v1.
6. `extensions/prompt-suggestion/README.md` — document behavior, configuration, acceptance flow, and limitations (widget-based UX, no true ghost text in v1).
7. `package.json` — add `extensions/prompt-suggestion` to `pi.extensions` so the extension loads automatically.
8. `extensions/prompt-suggestion/index.ts` — add guards so suggestions only appear when useful: no active run, no stale turn, no empty/low-signal conversation, and clear the widget when disabled or after acceptance.
9. `extensions/prompt-suggestion/index.ts` — add a lightweight read-only test harness path if feasible (pure helper functions for filtering / model-string parsing) so the critical logic can be exercised without interactive UI coupling.
Verify: npm test || npm run lint || true