# @aldoborrero/pi-btw

Ephemeral side questions that don't pollute context.

Like Claude Code's `/btw` command — ask a quick question without tool access, get an answer, and it doesn't become part of the conversation history.

## Usage

```
/btw What's the capital of France?
```

The agent answers without tool access, and the exchange is not stored in the session history.
