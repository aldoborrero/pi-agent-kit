# @aldoborrero/pi-footer

Custom footer renderer for the Pi TUI. Shows session metadata, token usage, and status indicators in the bottom bar.

## Usage

Automatically registered — no commands needed. The footer renders at the bottom of the terminal with model info, token counts, and session state.